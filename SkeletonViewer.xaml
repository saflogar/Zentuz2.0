<UserControl x:Class="Zentuz.SkeletonViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Loaded="UserControl_Loaded" Background="{x:Null}">
    <Grid x:Name="LayoutRoot">
        <Grid x:Name="SkeletonsPanel"/>
        <Canvas x:Name="HandCanvas">
            <Image x:Name="LeftHand" Source="Images/hand.png"
                       Visibility="Collapsed" Height="100" Width="100"/>
            <Image x:Name="RightHand" Source="Images/hand.png"
                       Visibility="Collapsed" Height="100" Width="100">
            </Image>

           
        </Canvas>
    </Grid>
</UserControl>
